{
    "servicerestart":"service caddy restart",
    "serviceget": "/usr/local/bin/nextcloudget",
    "serviceset": "/usr/local/bin/nextcloudset",
    "options":
	{
		"hostname": {
			"type": "string",
			"name": "Nextcloud FQDN",
			"description": "Hostname (FQDN) for Nextcloud",
			"requirerestart": true
		},
		"timezone": {
			"type": "string",
			"name": "Timezone",
			"description": "Timezone used for Nextcloud",
			"requirerestart": true
		},
		"cert_email": {
			"type": "string",
			"name": "E-Mail",
			"description": "Timezone used for Nextcloud",
			"requirerestart": true
		},
		"dns_env": {
			"type": "string",
			"name": "DNS Challenge Config",
			"description": "Settings for DNS challenge",
			"requirerestart": true
		}
	}
}
